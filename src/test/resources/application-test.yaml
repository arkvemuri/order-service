spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/orderdb_test
  autoconfigure:
    exclude:
      - org.springframework.cloud.netflix.eureka.EurekaClientAutoConfiguration

eureka:
  client:
    enabled: false

logging:
  level:
    org.springframework.data.mongodb: DEBUG
    com.codedecode.order: DEBUG